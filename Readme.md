# chip/ 
Содержит код задания 1 и пример использования, быстрый запуск:
```bash
$ cmake -B ./build && cmake --build ./build
$ cd build/ && ./chip
```
Если менять EEPROM на NOR, то главное отличие в записи. NOR память позволяет менять биты с помощью page program из состояния 1 в состояние 0, но не наоборот, единственный способ вернуть бит в состояние 1 - использовать одну из команд erase.

Но erase работает секторно (блочно) затирая большие объемы данных за раз (4, 32, 64Кб в W25Q128), в отличие от EEPROM где операции erase нет, а запись выполняется побайтово.

При этом выполнение erase иногда можно избежать, если уже записаная информация не конфликтует с новой. Пример,
```
0b10101001
&
0b00001000
=
0b00001000
```
# allocator/
Содержит код задания 2 и пример использования, быстрый запуск:
```bash
$ cmake -B ./build && cmake --build ./build
$ cd build/ && ./allocator
```

# Документация

Вы можете собрать документацию для каждого из проектов командой

```bash
$ doxygen Doxyfile
```

